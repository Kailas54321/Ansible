- name: Loop in Ansible
  hosts: all
  tasks:
    - name: loop
      debug:
        msg: 'My fav colour {{ itom }}'
      loop:
        - red
        - white
        - pink
        - black
        - skyblue

